---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - coffee-landing/app/layout.tsx
  - coffee-landing/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Layout shell renders correctly at mobile width (375px) with no horizontal overflow"
    - "Layout shell renders correctly at tablet width (768px) with no horizontal overflow"
    - "Layout shell renders correctly at desktop width (1280px) with no horizontal overflow"
    - "Brand fonts (Inter, Playfair Display) are loaded and applied"
  artifacts:
    - path: "coffee-landing/app/layout.tsx"
      provides: "Root layout with fonts, metadata, and body structure"
      contains: "next/font"
      min_lines: 20
    - path: "coffee-landing/app/page.tsx"
      provides: "Home page with layout shell structure"
      min_lines: 10
  key_links:
    - from: "coffee-landing/app/layout.tsx"
      to: "next/font/google"
      via: "font imports"
      pattern: "import.*from.*next/font/google"
    - from: "coffee-landing/app/layout.tsx"
      to: "coffee-landing/app/page.tsx"
      via: "children prop rendering"
      pattern: "\\{children\\}"
---

<objective>
Build the layout shell with mobile-first responsive structure and verify at all breakpoints.

Purpose: Establish the responsive foundation that Hero and About sections will build on.
Output: A responsive layout shell with proper fonts that adapts to mobile, tablet, and desktop.
</objective>

<execution_context>
@/Users/hesamabbaszadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hesamabbaszadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure fonts in layout.tsx</name>
  <files>coffee-landing/app/layout.tsx</files>
  <action>
Update layout.tsx to:

1. Import Inter and Playfair Display from next/font/google:
```typescript
import { Inter, Playfair_Display } from 'next/font/google'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-sans',
})

const playfair = Playfair_Display({
  subsets: ['latin'],
  variable: '--font-display',
})
```

2. Apply font CSS variables to the html element:
```typescript
<html lang="en" className={`${inter.variable} ${playfair.variable}`}>
```

3. Add basic metadata:
```typescript
export const metadata = {
  title: 'Coffee Shop',
  description: 'Modern specialty coffee experience',
}
```

4. Apply brand colors to body:
```typescript
<body className="bg-brand-cream text-brand-espresso font-sans">
  {children}
</body>
```
  </action>
  <verify>
Grep layout.tsx for "next/font/google" and "Playfair" to confirm font imports.
  </verify>
  <done>Root layout loads Inter and Playfair Display fonts with CSS variables and applies brand colors.</done>
</task>

<task type="auto">
  <name>Task 2: Create responsive layout shell in page.tsx</name>
  <files>coffee-landing/app/page.tsx</files>
  <action>
Replace page.tsx content with a minimal responsive layout shell:

```typescript
export default function Home() {
  return (
    <main className="min-h-screen">
      {/* Hero placeholder - will be replaced in Phase 2 */}
      <section className="h-screen flex items-center justify-center px-4 md:px-8 lg:px-16">
        <h1 className="font-display text-4xl md:text-5xl lg:text-6xl text-center">
          Coffee Shop
        </h1>
      </section>

      {/* About placeholder - will be replaced in Phase 3 */}
      <section className="py-16 px-4 md:px-8 lg:px-16">
        <h2 className="font-display text-3xl md:text-4xl mb-6">
          Our Story
        </h2>
        <p className="max-w-prose">
          Placeholder for the about section content.
        </p>
      </section>
    </main>
  )
}
```

Key mobile-first patterns:
- Base padding (px-4) increases at md: and lg: breakpoints
- Typography scales up at larger breakpoints
- max-w-prose constrains text width for readability
  </action>
  <verify>
Grep page.tsx for "md:" and "lg:" to confirm responsive classes are present.
  </verify>
  <done>Home page has responsive layout shell with mobile-first breakpoint patterns.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive layout shell with fonts and design tokens applied.</what-built>
  <how-to-verify>
1. Run `npm run dev` in coffee-landing directory
2. Open http://localhost:3000 in browser
3. Open DevTools (F12) and toggle device toolbar (Ctrl+Shift+M or Cmd+Shift+M)
4. Test at these widths:
   - Mobile: 375px - Text should be readable, no horizontal scroll
   - Tablet: 768px - Padding should increase, text size should grow
   - Desktop: 1280px - Comfortable margins, larger typography
5. Verify fonts load:
   - "Coffee Shop" heading should be in Playfair Display (serif)
   - Body text should be in Inter (sans-serif)
6. Verify brand colors:
   - Background should be cream colored
   - Text should be espresso (dark brown)
  </how-to-verify>
  <resume-signal>Type "approved" if layout looks correct at all breakpoints, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. Layout renders at 375px with no horizontal overflow
2. Layout renders at 768px with increased padding and text size
3. Layout renders at 1280px with comfortable margins
4. Playfair Display font visible in headings
5. Inter font visible in body text
6. Brand cream background and espresso text colors applied
</verification>

<success_criteria>
- Layout shell renders correctly at mobile (375px), tablet (768px), and desktop (1280px) widths
- No horizontal scrollbar at any breakpoint
- Fonts (Inter, Playfair Display) are loaded and applied correctly
- Brand colors (cream background, espresso text) are visible
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
