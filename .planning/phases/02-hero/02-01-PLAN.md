---
phase: 02-hero
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - coffee-landing/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Hero fills entire viewport on first load (no scroll needed to see it)"
    - "Tagline is clearly readable at all screen sizes"
    - "Background renders instantly without layout shift"
    - "Hero adapts cleanly to mobile, tablet, and desktop"
  artifacts:
    - path: "coffee-landing/app/page.tsx"
      provides: "Full-viewport hero section with tagline"
      contains: "min-h-dvh"
      min_lines: 15
  key_links:
    - from: "coffee-landing/app/page.tsx"
      to: "coffee-landing/app/globals.css"
      via: "Tailwind classes using design tokens"
      pattern: "bg-brand-espresso|text-brand-cream|font-display"
---

<objective>
Build the full-viewport hero section with color-based background and punchy tagline.

Purpose: Deliver the first visual impression of the coffee shop brand - bold, modern, design-forward.
Output: A complete hero section in page.tsx that fills the viewport, displays the brand tagline with proper contrast, and adapts cleanly across all breakpoints.
</objective>

<execution_context>
@/Users/hesamabbaszadi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hesamabbaszadi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero/02-RESEARCH.md
@coffee-landing/app/page.tsx
@coffee-landing/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement full-viewport hero with color background</name>
  <files>coffee-landing/app/page.tsx</files>
  <action>
Replace the hero placeholder section with a complete hero implementation:

1. Change the section from `h-screen` to `min-h-dvh` (prevents mobile browser chrome layout shift)
2. Use `bg-brand-espresso` for the background (dark coffee color from design tokens)
3. Center content both horizontally and vertically using flexbox
4. Add responsive padding: `px-4 sm:px-6 md:px-8 lg:px-16`

Structure:
```tsx
<section className="min-h-dvh flex items-center justify-center bg-brand-espresso px-4 sm:px-6 md:px-8 lg:px-16">
  <div className="text-center max-w-4xl">
    {/* Tagline goes here in Task 2 */}
  </div>
</section>
```

Keep the About placeholder section unchanged.
  </action>
  <verify>
Run `npm run dev` in coffee-landing directory. Hero section should:
- Fill the entire viewport height
- Have dark espresso background color
- Center any content
  </verify>
  <done>Hero section uses min-h-dvh, bg-brand-espresso background, and flexbox centering. No layout shift on mobile viewport resize.</done>
</task>

<task type="auto">
  <name>Task 2: Add responsive tagline with proper typography</name>
  <files>coffee-landing/app/page.tsx</files>
  <action>
Add the hero tagline content inside the centered div:

1. Main tagline (h1):
   - Text: "Craft Coffee, Elevated" (or similar punchy, differentiated copy)
   - Use `font-display` (Playfair Display) for brand identity
   - Use `text-brand-cream` for contrast against dark background
   - Responsive sizing: `text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl`
   - Add `leading-tight` for better line spacing on large text

2. Subheadline (p):
   - Text: "Where every cup is a journey" (supporting copy)
   - Use `text-brand-cream/80` (80% opacity for hierarchy)
   - Responsive sizing: `text-lg sm:text-xl md:text-2xl`
   - Add spacing: `mt-4 md:mt-6`
   - Constrain width: `max-w-2xl mx-auto`

Complete hero structure:
```tsx
<section className="min-h-dvh flex items-center justify-center bg-brand-espresso px-4 sm:px-6 md:px-8 lg:px-16">
  <div className="text-center max-w-4xl">
    <h1 className="font-display text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl text-brand-cream leading-tight">
      Craft Coffee, Elevated
    </h1>
    <p className="mt-4 md:mt-6 text-lg sm:text-xl md:text-2xl text-brand-cream/80 max-w-2xl mx-auto">
      Where every cup is a journey
    </p>
  </div>
</section>
```
  </action>
  <verify>
Run `npm run dev` and verify at multiple viewport sizes:
1. Mobile (375px): Text readable, no overflow, centered
2. Tablet (768px): Text scales up appropriately
3. Desktop (1280px+): Large, impactful typography

Check contrast: brand-cream (#f5f0e8 approx) on brand-espresso (#3d2f2a approx) should exceed 4.5:1 ratio.
  </verify>
  <done>Tagline displays "Craft Coffee, Elevated" with responsive sizing from text-3xl to text-7xl. Subheadline displays "Where every cup is a journey" at 80% opacity. Both are clearly readable at all breakpoints with proper hierarchy.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run dev` starts without errors
2. Hero fills full viewport height on mobile, tablet, and desktop
3. No layout shift when mobile browser chrome shows/hides (using dvh units)
4. Tagline is clearly readable at all screen sizes with sufficient contrast
5. Background is solid brand-espresso color (no image needed, stays under 200KB trivially)
6. About placeholder section still visible when scrolling down
</verification>

<success_criteria>
- HERO-01: Hero section fills viewport on first load (min-h-dvh applied)
- HERO-02: Punchy tagline "Craft Coffee, Elevated" readable at all sizes (responsive typography)
- HERO-03: Color background renders without layout shift, 0KB image cost
- Layout adapts cleanly to mobile (375px), tablet (768px), and desktop (1280px+)
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero/02-01-SUMMARY.md`
</output>
